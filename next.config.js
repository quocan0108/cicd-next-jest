/**
 * @type {import {'next'}.NextConfig}
 */

// next.config.js
const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    reactStrictMode: true
    }
}
// module.exports = nextConfig
  